import { decrementNum } from 'horror-calendar/helpers/decrement-num';
import { module, test } from 'qunit';

module('Unit | Helper | decrement num');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decrementNum([42]);
  assert.ok(result);
});
