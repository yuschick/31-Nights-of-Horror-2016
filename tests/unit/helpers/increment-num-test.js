import { incrementNum } from 'horror-calendar/helpers/increment-num';
import { module, test } from 'qunit';

module('Unit | Helper | increment num');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = incrementNum([42]);
  assert.ok(result);
});
