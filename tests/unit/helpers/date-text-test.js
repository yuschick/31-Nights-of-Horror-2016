import { dateText } from 'horror-calendar/helpers/date-text';
import { module, test } from 'qunit';

module('Unit | Helper | date text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateText([42]);
  assert.ok(result);
});
