import { displayNavArrows } from 'horror-calendar/helpers/display-nav-arrows';
import { module, test } from 'qunit';

module('Unit | Helper | display nav arrows');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayNavArrows([42]);
  assert.ok(result);
});
