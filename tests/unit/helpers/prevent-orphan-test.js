import { preventOrphan } from 'horror-calendar/helpers/prevent-orphan';
import { module, test } from 'qunit';

module('Unit | Helper | prevent orphan');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = preventOrphan([42]);
  assert.ok(result);
});
